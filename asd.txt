Jag har två stycken gallerys (Items) som fuingerar, men jag behöver hjälp att korrigera så att det inte ger "Delegation warning. The "StartsWith" part of this formula might not work correctly on large data sets.". Det gäller specifikt delen med "StartsWith(LSP,Text(ThisRecord.LSP))" - Hur kan du hjälpa mig att korrigera detta?

Här är båda fullständiga fx-koderna till galleriernas Items:

galOngoing.Items:

With({spBase:If(IsBlank(txtSearch.Text),Filter([@'Cepheid | Production orders'],(StartsWith('Order#',"111")||StartsWith('Order#',"112"))&&'ROBAL - Actual start date/ time'>=varMinDate&&'IPT - Testing finalized'=Blank()),Filter([@'Cepheid | Production orders'],(StartsWith('Order#',"111")||StartsWith('Order#',"112"))&&'ROBAL - Actual start date/ time'>=varMinDate&&'IPT - Testing finalized'=Blank()&&(StartsWith('Order#',txtSearch.Text)||StartsWith('SAP Batch#',txtSearch.Text)||StartsWith('SAP Batch# 2',txtSearch.Text)||StartsWith(LSP,txtSearch.Text))))},With({enrichedBase:AddColumns(spBase,MaterialText,'Material'.Value,WorkCenterTextLocal,'Work Center'.Value,RobalNoLocal,IfError(Value(Substitute(Substitute('Work Center'.Value,"ROBAL ",""),"ROBAL","")),Blank()),SqlBagTested,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),'BagTested'),SqlErrors,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),ERRORS),SqlMajorErrors,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),MajorErrors),SqlSTF,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),STF))},AddColumns(colRobalLines As line,Run,First(Sort(Filter(enrichedBase,RobalNoLocal=line.RobalNo),'ROBAL - Actual start date/ time',SortOrder.Descending)))))

galActionQueue.Items:

With({spBase:If(IsBlank(txtSearch.Text),Filter([@'Cepheid | Production orders'],(StartsWith('Order#',"111")||StartsWith('Order#',"112"))&&'Production Day/ Time'>=DateAdd(Today(),-45,TimeUnit.Days)&&(('IPT - Testing finalized'>=varMinDate&&'IPT - Compiling of data'=Blank())||('IPT - Compiling of data'>=varMinDate&&'IPT - Document review'=Blank()))),Filter([@'Cepheid | Production orders'],(StartsWith('Order#',"111")||StartsWith('Order#',"112"))&&'Production Day/ Time'>=DateAdd(Today(),-45,TimeUnit.Days)&&(('IPT - Testing finalized'>=varMinDate&&'IPT - Compiling of data'=Blank())||('IPT - Compiling of data'>=varMinDate&&'IPT - Document review'=Blank()))&&(StartsWith('Order#',txtSearch.Text)||StartsWith('SAP Batch#',txtSearch.Text)||StartsWith('SAP Batch# 2',txtSearch.Text)||StartsWith(LSP,txtSearch.Text))))},Sort(AddColumns(spBase,ActionStatus,If('IPT - Testing finalized'>=varMinDate&&'IPT - Compiling of data'=Blank(),"Waiting compiling",'IPT - Compiling of data'>=varMinDate&&'IPT - Document review'=Blank(),"Waiting review",""),MaterialText,'Material'.Value,WorkCenterTextLocal,'Work Center'.Value,SqlBagTested,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),'BagTested'),SqlErrors,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),ERRORS),SqlMajorErrors,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),MajorErrors),SqlSTF,LookUp('IPT Ongoing',StartsWith(LSP,Text(ThisRecord.LSP)),STF)),'Production Day/ Time',SortOrder.Ascending))
